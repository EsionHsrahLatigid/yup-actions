paths:
  .github/workflows/plugin-release-signed.yml:
    ignore:
      # GitHub documents these reusable-workflow job identity properties, but
      # actionlint 1.7.12 does not yet include them in its job context schema.
      - 'property "workflow_repository" is not defined in object type'
      - 'property "workflow_sha" is not defined in object type'
